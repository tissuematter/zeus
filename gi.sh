git config --global user.name 'zeus'
git config --global user.email 'zeus@tissuematter.com'
git init
git add .
git commit -m "sh git setup"
git remote add origin https://github.com/tissuematter/zeus.git
git push -u origin master

